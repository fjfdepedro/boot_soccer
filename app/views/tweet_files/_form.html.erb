<%= form_with(model: tweet_file, local: true) do |form| %>
  <% if tweet_file.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tweet_file.errors.count, "error") %> prohibited this tweet_file from being saved:</h2>

      <ul>
      <% tweet_file.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :text %>
    <%= form.text_field :text, id: :tweet_file_text %>
  </div>

  <div class="field">
    <%= form.label :date %>
    <%= form.datetime_select :date, id: :tweet_file_date %>
  </div>

  <div class="field">
    <%= form.label :coordinates %>
    <%= form.text_field :coordinates, id: :tweet_file_coordinates %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
